node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Local SQLite database artifacts — never commit these
*.db
*.db-shm
*.db-wal

# Precompiled native binaries — build on-demand or fetch as release assets
*.node
